# Database Configuration
DATABASE_URL=postgresql://myuser:mypassword@db:5432/brain_dev

# Filestore Configuration
# Set this to the path where your NIfTI files are stored
# For local development, this might be a relative path
# For production deployment, this should be the mounted volume path
FILESTORE_PATH=/app/filestore

# Optional: Override for different environments
# FILESTORE_PATH=/mnt/nifti_filestore  # For production with mounted volume
# FILESTORE_PATH=./filestore           # For local development 